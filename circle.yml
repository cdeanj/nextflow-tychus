machine:
  java:
    version: openjdk8
  services:
    - docker

test:
  override:
    - gunzip tutorial/raw_sequence_data/*
    - curl -fsSL get.nextflow.io | bash
    - ./nextflow run assembly.nf -profile assembly --threads 4 --output test:
        timeout: 2400
        
